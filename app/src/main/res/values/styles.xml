<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--  text styles  -->
    <style name="TextStyle">
        <!--TODO -->
    </style>

    <style name="TextStyleContentTitle" parent="TextStyle">
        <!--TODO   <item name="fontPath">@string/font_path_bold</item> -->
        <!--TODO  <item name="android:textSize">@dimen/text_size_content_title</item> -->
    </style>

    <!--Alert Dialog-->
    <style name="AlertDialogFragment" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
